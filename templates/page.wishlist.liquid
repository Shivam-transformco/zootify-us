<section class="wishlist-page page-width">
  <header class="wishlist-header">
    <h1 class="wishlist-title">My Wishlist</h1>
    <p id="wishlist-empty" class="wishlist-empty" hidden>
      Your wishlist is empty.
    </p>
  </header>

  <div
    id="wishlist-products"
    class="wishlist-grid"
    aria-live="polite"
  ></div>
</section>
